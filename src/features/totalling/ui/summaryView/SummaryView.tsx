import { Stack } from "@mui/material"

type Props = {    

}

// const aaa = (comp: SummaryComposition, val: PerformanceObject[]) => {

//     //IDと項目名のセット
//     const sm = comp.summaryItems.map(smitem => ({ id: smitem.id, name: smitem.name }));

    
//     const a = sm.map(sum => {

//         const b = val.map(monthData => {

//             // const abc = monthData.budget[0]

//             // monthData.budget[0]

//         });

//         return 0;
//     });

//     // const ss = sm.map(i => )

// // val[0].actual[0]

//     console.log(sm);

// }

export const SummaryView = (props: Props) => {    

    // const { monthsList, composition } = props;
    // const data = {

    // }

    // aaa(composition);

    // const a = composition.summaryItems.map(smitem => smitem.name);
    // const b = months.map(monthData => {

    //     // monthData.budget.map(i => i.summaryValues)

    // });

    return <>
        <Stack>
            {/* { monthsList.map(item => <div>{item}</div>) } */}
        </Stack>
    </>
}